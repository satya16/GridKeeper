.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
grid.db
.config/grid-worker/

# Frontend build output -- generated by `npm run build` in manager/frontend/,
# served from here by manager/app/main.py. Not committed, same as any other
# build artifact; run the build (see manager/frontend/README or the repo
# README's quickstart) to regenerate it.
manager/app/static/dist/
manager/frontend/node_modules/
